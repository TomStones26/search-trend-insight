# Google Trends US 数据源可行性验证报告

**结论：可行，而且比预想简单。已跑通端到端，不是纸上推演。**

验证时间：2026-09-16 · 数据源：`https://trends.google.com/trending?geo=US`

---

## 1. 实测结果

| 项目 | 结果 |
|---|---|
| 抓取方式 | 单个 HTTP GET，**不需要浏览器、不需要 API Key、不需要登录** |
| 单次返回热点数 | **299 条**（全量，非分页） |
| 单次耗时 | **约 1.5 秒** |
| 每条含字段 | 主题、搜索量、增长率、开始时间、最多 84 个关联搜索词、新闻引用 |
| 反爬情况 | 无验证码、无限速拦截。已按 ≥1s 间隔自律请求 |
| 成本 | 免费 |
| 稳定性 | 连续 2 次采集均成功，字段结构一致 |

已产出可直接使用的采集器：`src/gt_collect.py`（纯标准库，无第三方依赖）。

---

## 2. 为什么能不靠浏览器抓到

这是本次验证最有价值的技术发现：

**Google 把完整数据以 `AF_initDataCallback` 的形式内嵌在 HTML 源码里。**

```html
<script>AF_initDataCallback({key: 'ds:0', hash: '2', data:[null,[
  ["eliana moreno", null, "US", [1789525800], null, null, 100000, null, 1000, [...40个关联词], ...],
  ["dancing with the stars", null, "US", [1789503000], null, null, 500000, null, 1000, [...],
  ...共 299 条
]]});</script>
```

也就是说：页面虽然是个 SPA，但**首屏 HTML 里已经带了全量数据**，只是用 JS 数组存着没渲染。所以只要把这个数组抠出来解析就行。

这一条直接砍掉了原方案里最重的部分：

| 原以为需要 | 实际需要 |
|---|---|
| 无头浏览器 / Playwright | 一个 `urllib.request` |
| 等待 JS 渲染、模拟滚动 | 一次 GET |
| 破解 batchexecute 签名接口 | 无需调用接口 |
| API Key / 代理池 | 都不需要 |

顺带一提，历史上常用的两个老接口 `trends/api/realtimetrends` 和 `trending/api/trending` **已经 404 下线了**——网上很多教程还在教这两个，别踩。

---

## 3. 字段对照：你要的 vs 实际能拿到的

`ds:0` 是位置数组、没有字段名，下面是我逐字段 profiling 299 条记录后确认的映射（并已用页面渲染结果交叉验证）：

| 数组位置 | 实际含义 | 示例值 | 对应你 spec 的字段 |
|---|---|---|---|
| `[0]` | 热搜主题 | `"dancing with the stars"` | `trend` ✅ |
| `[2]` | 地区 | `"US"` | — |
| `[3][0]` | 开始成为趋势的时间（unix 秒） | `1789503000` | `started_at` ✅ |
| `[4][0]` | 未知时间戳，59/299 条为 null | `1789548000` | 待定，暂存 `raw_ts4` |
| `[6]` | **搜索量**（12 档桶值） | `500000` | `search_volume` ✅ |
| `[8]` | **增长率 %**（相对预测基线） | `1000` | `growth_rate` ✅ |
| `[9]` | **关联搜索词**（最多 84 个） | `["dwts","dwts elimination",...]` | `trend_breakdown` ✅ |
| `[10]` | 未知整数数组 | `[11]` | — |
| `[11]` | 新闻文章引用 `[id, lang, country]` | `[[4832205704,"en","US"]]` | 可做新闻溯源 |
| `[12]` | 主题（与 `[0]` 重复） | — | — |

**验证方式**：页面渲染出的那一行文本是
`eliana moreno | 100K+ searches | trending_up | Active | 7h ago | 100K+ | arrow_upward | 1,000%`
——`100K+` 对上 `[6]=100000`，`1,000%` 对上 `[8]=1000`。字段映射确认无误。

---

## 4. 三个数据缺口（必须知道，否则会踩坑）

### 缺口 1：`status`（Active / Ended）源站不给

页面只渲染当前活跃的热点，`ds:0` 里没有状态字段（全文档 `>Ended<` 出现 0 次）。

**对策已实现**：靠历史快照自行判定——本轮在榜且上轮也在 → `Active`；上轮在榜本轮消失 → `Ended`。
这里有个坑我在实测中踩到了：**判定必须限定在「同地区 + 同时间窗口」内**，否则把时间窗口从 24h 换成 4h，会导致一大批热点被误判为 Ended（我第一版就误判了 275 条）。已修正。

### 缺口 2：`category`（类别）源站不给列表级字段

页面有类别筛选器，但每条热点不带类别标签。

**对策：自建分类，已解决。**

第一版用「首个命中即返回」的关键词规则，实测效果**不达标**——
299 条里 233 条落到 `Other`（77.9%），只识别出 5 个品牌。

M0 换成 **`config/taxonomy.json` 加权打分词典**（LLM 依据真实样本构建）：

| 指标 | 首命中规则 | 加权打分词典 |
|---|---|---|
| Other 占比 | 77.9% | **13.4%** |
| 识别品牌数 | 5 | **16** |
| 消费相关热点 | — | 31 条（10.4%） |

> ⚠️ 上表是**样本内**结果：那 299 条正是 LLM 用来建词典的样本。
> 后来用同一份词典回刷线上新数据（330–333 条），Other 实测 **20.4% ~ 23.2%**，
> 与样本内相差约 **9.8 个百分点** —— 存在泛化差距。对外描述请用样本外数字，
> 或两个都写。详见 BUILD.md「词典打标质量」一节。

改了四点：① 加权打分取代首命中；② 热点名权重 1.0、关联搜索词权重 0.5（名称模糊
时关联词能救回来，例如「above」靠关联词里的 `stock market today` 被正确判为金融）；
③ 输出多标签；④ 保留命中词，可解释、可迭代。

剩余 13.4% 的 Other 逐条看过，**基本是纯人名和无语义碎片**，这类需要实体识别能力，
不是堆词典能解决的。

词典是**可迭代的活资产**，不是一次性产物：
`python src/classify.py --samples` 看标注质量 → 补词 → `--relabel` 回刷历史数据。

### 缺口 3：所有"变化类"指标都需要时间累积

这些指标**单次快照拿不到**，只能靠定时采集攒：

| 指标 | 需要攒多久 |
|---|---|
| `first_seen_at` / `last_seen_at` | 2 次快照起 |
| `duration_hours` / `persistence` | 2 次快照起（已验证可用） |
| New Trends Timeline（每小时新增） | 1 天 |
| Category Trend Over Time | 3–7 天 |
| 真正的"生命周期曲线" | 1–2 周 |

**好消息**：这部分不需要额外开发，只需要**让它定时自动跑**。采集器已经能算出 `persistence` 和 `duration_hours`，验证时第二次运行 299 条全部正确累加为 `persistence=2`。

### 另外两个口径注意点

- **搜索量是桶值，不是精确值**。只有 12 个档位（100 / 200 / 500 / 1K / 2K / 5K / 10K / 20K / 50K / 100K / 200K / 500K）。所以「搜索量合计」只能叫**估算量级**，不能宣称精确总量。
- **增长率也是离散档位**（50 / 75 / 100 / 200 / 300 … 1000）。所以「增长率中位数」实际是在桶值上取中位，解读时要说清。

---

## 5. 建议的简化范围

你说可以简单一些——我认为**该砍的不是功能，而是顺序**。砍顺序的依据是上面三个缺口：依赖历史累积和 LLM 打标的部分，硬做只会得到一张全是 `Other` 的空洞看板。

### M0 · 一天可交付（已验证可行）

只做**一页看板**，把 spec 里 Page 1 和 Page 2 的核心压缩在一起：

| 模块 | 数据来源 | 状态 |
|---|---|---|
| KPI 卡 ×6（热点数 / 搜索量合计 / 增长率中位数 / 搜索量中位数 / 消费热点 / 平均关联词数） | 单次快照 | ✅ 已实现 |
| 搜索量 Top 15（横向柱状） | 单次快照 | ✅ 已实现 |
| 增长最快 Top 15（横向柱状） | 单次快照 | ✅ 已实现 |
| **搜索量 × 增长率 四象限散点** | 单次快照 | ✅ 已实现 |
| 类别结构（环形 + 柱状） | 需 LLM 打标 | ⚠️ 规则版仅占位 |
| 新兴热点表格 | 单次快照 + 阈值 | ✅ 已实现 |
| 「更新数据」按钮 + 采集器 | — | ✅ 已实现 |

> 上面这些**现在已经能跑**，见 `demo/dashboard-demo.html`（已用无头 Chrome 实际渲染验证）。

### M1 · 一周内（加上时间维度）

- 类别识别换成 LLM 打标 → 解锁「类别结构」「消费洞察」两页
- 定时每小时采集 → 解锁「生命周期」「新增热点时间分布」
- 消费/品牌识别升级（品牌词典 + LLM 判定）

### M2 · 完整形态

- 专项分析报告自动生成（接 llm 解释「为什么突然火了」：关联词 + 新闻引用 + 外部新闻）
- 四页完整版 + 历史对比

---

## 6. 页面裁剪建议

你的 spec 设计得很好，但对 M0 来说 4 页里有 3 页是空的：

| 原 spec 页面 | M0 建议 | 原因 |
|---|---|---|
| Page 1 Market Overview | **做**（含四象限） | 全部字段单次可获取 |
| Page 2 Trend Analysis | **拆一半进来** | 四象限可做；生命周期/持续度暂时没数据 |
| Page 3 Category & Topic | **推迟** | 依赖 LLM 打标 |
| Page 4 Commerce Insights | **推迟到 M1** | 依赖 LLM 打标 + 时间累积 |

**一句话**：M0 先用「搜索量 × 增长率 四象限」这一张图立住产品价值——它已经能回答「哪些是爆发型、哪些是成熟型、哪些是潜在型」，这是你 spec 里最亮的分析点，且完全不需要历史数据。

---

## 7. 风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| Google 改版，`ds:0` 结构变化 | 中 | 采集器对结构敏感：解析失败会明确报错而不是静默返回空；关键字段做非空校验 |
| 被判定为爬虫限流 | 低 | 已内建 ≥1s 间隔；建议单日采集不超过 24 次（每小时 1 次） |
| 类别打标不准 | 高（现状） | M0 明确标注为"规则版占位"，M1 换 LLM |
| 搜索量桶值被误当精确值 | 中 | 所有展示加「估算」标注，已在 demo 页脚声明 |
| 快照文件互相覆盖 | 已解决 | 文件名含 `geo + 窗口 + 秒`，实测两次连跑生成两个独立快照 |

---

## 8. 本地运行方式

```bash
# 采集（默认 US / 最近 24 小时）
python src/gt_collect.py

# 指定窗口
python src/gt_collect.py --geo US --hours 4

# 生成看板
python src/build_demo.py

# 打开 demo/dashboard-demo.html
```

产出文件：
- `data/snapshots/snapshot_US_24h_<时间>.jsonl` — 每次采集的原始快照（事实表）
- `data/dim_trend.jsonl` — 趋势维度表（含 first_seen / duration / persistence / status）
- `data/latest.json` — 最新一次结果，看板读这个

三张表的结构和你 spec 第 7 节的设计一致（`fact_trends_snapshot` / `dim_trend`），
`fact_related_queries` 目前内嵌在快照的 `related_queries` 数组里，需要拆表时再说。

---

## 9. 一句话总结

**爬虫这一关已经过了，而且是免费、免密钥、1.5 秒拿全量 299 条。**
真正的工作量不在采集，在两件事：**用 LLM 做类别/品牌打标**，和**让它定时跑起来攒历史**。
前者决定看板内容有没有深度，后者决定生命周期分析能不能成立。

---

## 10. 补充：受限网络下的行为（M0 实测新增）

验证过程中环境的代理一度把 `trends.google.com` 拦成 `502 Bad Gateway`
（同一时刻 `example.com` 返回 200，说明是目标域名白名单拦截，不是断网）。

这条实测带来一个必须写进产品的要求：**采集失败必须降级，不能假装成功。**

现在的实现：

| 情况 | 行为 |
|---|---|
| 线上抓取成功 | `source = "live"`，页面绿色徽章 |
| 线上抓取失败 | 自动回退到本地最近快照，`source = "cached"`，页面黄色徽章 + 日志写明原因 |
| 线上失败且本地无快照 | 明确报错，不静默返回空数据 |

在用户本机（无代理白名单限制）直连正常，本轮 6 次采集全部为 `source = "live"`，
单次 332 条、耗时约 1.5 秒。

**结论不变：数据源本身可用。** 这条补充是为了让「抓不到的时候产品怎么表现」有明确答案。

---

## 11. M0 完成情况

M0 范围（见上文第 5 节）已全部实现，并额外完成了原计划放到 M1 的部分：

| 模块 | 计划 | 实际 |
|---|---|---|
| KPI 卡 ×6 | M0 | ✅ |
| 搜索量 Top 15 / 增长最快 Top 15 | M0 | ✅ |
| 搜索量 × 增长率 四象限 | M0 | ✅ |
| 新兴热点表 | M0 | ✅ |
| 四页面壳（主页/分析过程/看板/报告） | M0 | ✅ |
| **类别结构（环形 + 柱状）** | M1 | ✅ 提前完成 —— 加权词典把 Other 压到 13.4%，可用 |
| **消费与品牌洞察** | M1 | ✅ 提前完成 —— 43 品牌表 + 消费子类 |
| **关联搜索词网络图** | M2 | ✅ 提前完成 |
| 生命周期（时长分布 / 持续度） | 攒数据后 | ⚠️ 技术可用，样本间隔太短无业务意义 |
| 需求输入 → 计划解析 → 分范围分析 | — | ✅ 新增，规则解析器 + LLM 双通道 |

详细验证结果与踩到的 bug 见 `BUILD.md`。

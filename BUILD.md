# M0 交付与验证报告

**日期**：2026-09-16 · **范围**：M0（四页面壳 + 分类词典 + 分析引擎 + 本地服务
+ 定时采集 + token 成本实测）

这份文档只写**实测到什么、修了什么、没验证什么**，不写设计意图。
设计意图看 `README.md`，数据源细节看 `FEASIBILITY.md`。

---

## 1. 交付物

| 层 | 文件 | 状态 |
|---|---|---|
| 页面 | `index.html` / `analysis.html` / `dashboard.html` / `report.html` | ✅ 无头 Chrome 实际渲染验证 |
| 样式 | `assets/theme.css` / `assets/common.js` | ✅ 鲜亮配色，浅色底 |
| 服务 | `server.py` | ✅ 零依赖，15 个路由全部实测 200 |
| 编排 | `src/pipeline.py` | ✅ 六阶段 + 进度落盘 + 「只采集」路径 |
| 调度 | `src/scheduler.py` | ✅ 定时采集 + 进程内开关 + 基线重建 |
| 采集 | `src/gt_collect.py` | ✅ 325–333 条 / 1.1–2.2 秒 |
| 打标 | `src/classify.py` + `config/taxonomy.json` | ✅ Other 占比 77.9% → 13.4%（样本内）/ ≈23%（样本外，见下） |
| 分析 | `src/analyze.py` | ✅ 需求解析 + 8 类多维计算 + 结论生成 |
| 报告 | `src/report.py` | ✅ 7 章 |
| LLM | `src/llm.py` | ✅ 已用本地 mock 端点端到端验证（28/28），含 6 服务商预设与自检 |
| 配置 | `config/llm.env.example` | ✅ 免环境变量的密钥配置模板 |
| 测试 | `tests/test_llm_path.py` | ✅ 6 个场景 28 项断言 |
| 测试 | `tests/measure_tokens.py` | ✅ token 消耗实测（截获真实 prompt） |

---

## 2. 实测数字（2026-09-16 21:35 那次运行）

网络放开后复测，直连正常：

```
采集      done   325 条热点（实时）                    1361 ms
清洗      done   质量分 1.0，6 项检查                     24 ms
打标      done   词典 v1.0.0，覆盖 13 类                  81 ms
指标      done   KPI 6 项，图表数据 13 组                 88 ms
分析      done   筛选后 22 条，8 条结论                  142 ms
报告      done   7 个章节，叙述引擎 rule                 134 ms
```

`source=live`、`save_error=None`、总耗时约 1.9 秒。同时确认 6 个主流 LLM 端点
（OpenAI / DeepSeek / DashScope / Moonshot / 智谱 / SiliconFlow）全部网络可达
（返回 401 而非超时，即只差密钥）。

需求：*「看看科技、消费和游戏板块里增长最快、最值得跟的热点，重点找品牌和商品层面的机会」*

系统解析为：围绕「科技、游戏、消费与零售」，重点 `增长动能 + 消费与品牌信号 + 内容结构`，
命中 22 / 325 条。

### 词典打标质量（299 条样本 · **样本内**）

> ⚠️ 下表的 13.4% 是在**建词典用的那 299 条**上测的，属于样本内成绩。
> 后续复核实测：同一份词典回刷**线上新采集**数据（330–333 条），
> Other 为 **20.4% ~ 23.2%**，与样本内差 **约 9.8 个百分点**。
> 对外引用时以样本外为准，或两个数都写。复核命令见 README「分类词典」一节。

| 指标 | 改前（首命中规则） | 改后（加权打分词典） | 样本外实测 |
|---|---|---|---|
| Other 占比 | 77.9% | **13.4%** | **≈23.2%** |
| 识别品牌数 | 5 | **16** | — |
| 消费相关热点 | — | 31 条（10.4%） | — |
| 多标签条数 | 0 | 8 | — |

剩余 13.4% 的 Other 我逐条看过：**基本是纯人名（`uju anya`、`yuri nakamura`）
和无语义碎片（`o`、`w`、`to`、`against`）**。这类需要实体识别能力，
靠关键词词典到此为止 —— 这是诚实的天花板，不是调参能解决的。

样本外差距进一步印证了同一件事：**未归类的绝大多数是新出现的专名/人名**，
词典对新词没有泛化能力。要做对，正确路径是加实体识别和词典的定期增量迭代，
而不是继续堆关键词。

### 服务接口

15 个路由全部 200：`/api/{meta,status,dataset,analysis,report,dashboard,quality,schedule,schedule_state}`
+ 4 个页面 + 2 个静态资源。

---

## 3. LLM 通道验证（本地 mock 端点，不需要密钥）

上一轮遗留的唯一未验证项是 LLM 路径。没有密钥时的正确做法不是把话说含糊，
而是用 mock 端点把代码路径真正跑一遍 —— 它证明的是**代码**通，不是某个模型的回答好。

`tests/test_llm_path.py` 在本地起一个 OpenAI 兼容端点（自动选空闲端口），
把 `OPENAI_BASE_URL` 指过去，然后调用真实的 `pipeline.run_pipeline()`。

结果：**28/28 通过**。

| 场景 | 构造 | 期望 | 实测 |
|---|---|---|---|
| 01 正常返回 | 返回合法 JSON | `intent=llm`，plan 生效 | ✅ 2.4s |
| 02 围栏包裹 | ` ```json … ``` ` | 宽松解析生效 | ✅ `intent=llm` |
| 03 非 JSON 垃圾 | 返回「抱歉，我无法完成」 | 降级 rule，不中断 | ✅ `done` |
| 04 上游 500 | HTTP 500 | 降级 + 记录 last_error | ✅ `HTTPError: 500` |
| 05 响应超时 | 拖过客户端超时 | 降级 + 记录 last_error | ✅ `TimeoutError: timed out` |
| 06 `response_format` 被拒 | 带该参数就 400，去掉就 200 | 自动重试，仍为 `llm` | ✅ 重试 4 次全成功 |

场景 06 是这轮新加的：**部分 OpenAI 兼容服务商不支持 `response_format`**，
原本会直接 400 → 静默降级成规则模式，用户只会感觉「模型没生效」却查不出原因。
现在遇到 400 会自动去掉该参数重试一次，重试次数记在 `llm.retries` 里。

关键断言不是「有没有报错」，而是**模型真的驱动了结果**：

- 需求文本「帮我盯一下这波热度里哪些方向最值得下注」**不含任何类别关键词**，
  规则解析器必然得出空筛选；而实测结果是 `categories = ["Technology","Consumer"]`、
  `min_growth = 500`、报告总述逐字等于 mock 返回的 `【MOCK】…`。
  这排除了「其实还是规则在跑」的可能。
- 请求侧逐项核对：`Authorization: Bearer …`、`response_format={"type":"json_object"}`、
  `model` 透传、路径 `/v1/chat/completions`、可用类别清单确实注入了 system prompt。

### 顺带补掉的两个真实缺口

**缺口 A：`engine.llm` 原本只在开跑前快照一次。** 后果是：配了密钥但运行中调用失败时，
UI 会一直显示「LLM 已接入」，而实际上每一次都在降级 —— 用户完全看不出来。

现在改为在报告阶段结束后回写真实状态（含 `calls` 调用次数与 `last_error`），
并在日志里写明「LLM 调用异常，已降级为本地规则」。

**缺口 B：`llm.py` 少了超时可配项。** 原本只能改配置文件，测试和部署都不方便。
现已支持 `OPENAI_TIMEOUT_SECONDS`，并新增 provider 预设与 `config/llm.env` 读取。

**留下的边界**：没有真实密钥，所以**没验证过任何真实模型的输出质量**。
mock 只能证明管道通，不能证明「某个模型的需求理解够准」。

---

## 4. token 消耗实测 + 定时采集

### 4.1 结论先说

| 操作 | 模型调用 | 输入 | 输出 |
|---|---|---|---|
| 定时采集（每小时一轮） | **0 次** | **0** | **0** |
| 「更新数据」按钮 | **0 次** | **0** | **0** |
| 提一个问题（完整分析） | 2 次 | 约 **1.2k** | 约 **0.4k** |

**定时采集不花 token，一次都不花。** 理由很简单：它只跑前四个阶段，
而前四个阶段全部是本地计算 —— 采集是 HTTP 请求、清洗和质量检查是本地规则、
类别与品牌走 `config/taxonomy.json` 词典（不是让模型逐条分类）、指标是纯 Python 算术。
唯一调用模型的两步（需求解析、报告叙述）在定时路径里被显式跳过。

所以「每小时采一次、跑一年」= 8760 轮 = ¥0。

### 4.2 怎么量的（不是估算）

`tests/measure_tokens.py` 起本地 mock 端点，把 `OPENAI_BASE_URL` 指过去，
跑真实 `run_pipeline()`，截获两次请求的 system + user 全文，逐字符数出来：

```
调用 1 · 需求理解    system 101 中文字 + 348 其他字符
                     user   320 中文字 + 474 其他字符
                     → 约 499 tok（DeepSeek 口径）

调用 2 · 报告叙述    system 112 中文字 + 136 其他字符
                     user   111 中文字 + 1910 其他字符
                     → 约 748 tok（DeepSeek 口径）
```

按 DeepSeek `deepseek-flash` 现价（空闲时段输入 ¥1/M、输出 ¥4/M）：
**单次提问约 ¥0.003**；一天问 100 次约 ¥0.3。
即使用最贵的 `deepseek-v4-pro` 高峰价，单次也只有约 ¥0.02。

这个量与「数据量」关系很小 —— 报告叙述把已算好的指标摘要喂进去，
不是把 325 条原始记录全塞进 prompt，所以热点涨到 500 条也不会显著变贵。

### 4.3 为什么要把「更新数据」也改成零 token 路径

原来「更新数据」按钮调的是 `start_run("")` → 完整六阶段，
用默认需求跑一次需求解析 + 报告 —— 也就是说**每次点刷新都在花 token，
而且产出的还是一份跟本次刷新无关的默认报告**。

现在拆成两个入口：

| 入口 | 跑到哪 | 位置 |
|---|---|---|
| `run_pipeline()` | 全部 6 阶段 | `POST /api/analyze`（提问） |
| `run_collect_only()` | 只到阶段 4 | `POST /api/collect`（刷新）、scheduler |

`/api/collect` 的实测返回就带上了这个承诺：

```json
{"ok": true, "message": "已启动", "tokens": 0,
 "hint": "刷新数据不含需求分析，不消耗 token；进度见 /api/status"}
```

### 4.4 定时采集的实现要点

- **独立状态文件。** 定时采集写 `data/schedule_state.json`，
  绝不覆盖用户手动分析留下的 `data/status.json` —— 否则你在分析页看着进度，
  后台一小时一次的采集会把它顶掉。已实测确认：采集跑完后 `status.json`
  的 `kind` 仍是 `analysis`，需求文本一字未变。
- **页面上可开关。** 主页「自动采集」卡片 → `POST /api/schedule/start`，
  在服务进程内起后台线程。停止用 `POST /api/schedule/stop`，
  实现是可被立刻打断的 `Event.wait()`，不用干等到下一轮；
  当前那一轮采集会跑完，不会半途丢数据。
- **对齐间隔边界 + 抖动。** 采集时间对齐到间隔边界（便于按小时做时间序列），
  并加 0–90 秒随机抖动，避免整点扎堆。
- **安全阀。** 最小间隔 15 分钟、每轮请求间隔 ≥ 1 秒、每日上限默认 30 次、
  失败自动降级到本地快照、`--once` 模式可交给系统计划任务。

### 4.5 基线的坑（我踩了）

测试期间我连续跑了十几次采集，`data/snapshots/` 攒到 45 期 —— 但**这些快照
都在十几分钟内产生**。于是看板上「连续在榜 45 次」这个数字完全失真：
`persistence` 的单位是**采集批次**，不是小时，批次间隔不等时它就没有业务含义。

为此加了 `--reset-history`：把旧快照**归档**（移到
`data/snapshots/_archive_<时间戳>/`，不是删除）、`dim_trend.jsonl` 改名保留、
计数归零，然后立刻采一次新基线。

```
已归档 45 期历史快照 → data\snapshots\_archive_20260916T213116
dim_trend.jsonl 已保留为 .bak，计数已归零
[21:31:18] 完成：330 条，来源 live，2.19s，累计快照 1 期
新基线已建立，之后按固定间隔累积即可得到有意义的生命周期指标。
```

**正式启用定时采集前应该先跑一次 `--reset-history`。**

### 4.6 接口联调（全部通过）

一次跑完 5 组断言：

```
1. 接口可用性        15 个路由全部 200（页面 + 静态资源 + 9 个 API）
2. 立即采一次        330 条 / source=live / 运行次数 +1 / LLM 调用 0
3. 更新数据按钮      tokens=0；首次轮询即 running（无竞态）；save_error=None；
                     步骤 1-4 done、5-6 skipped；需求文本被保留；kind=collect
4. 定时采集开关      start / 重复 start 被拒 409 / stop 后线程停止且清空 next_run_at
5. 完整分析          六阶段 done、source=live、命中 22 条、8 条结论、7 章报告
```

---

## 5. 踩到并修掉的两个真实 bug

### bug 1：状态文件原子写冲突 → 任务永久卡在 `running`

**现象**：通过 API 触发分析后，`status.json` 显示 `state=running, progress=0`，
六个阶段全是 `pending`，**日志一条都没有**，且永久不变。但用命令行直接跑同一个
流水线却 2 秒正常完成。

**定位过程**：发现 `data/status.json.tmp` 残留。原实现是固定临时文件名：

```python
tmp = path + ".tmp"
json.dump(obj, open(tmp, "w"))
os.replace(tmp, path)
```

服务线程每秒轮询读 `status.json`，后台线程高频写。Windows 上 `os.replace` 在目标
文件被读取方短暂占用时会抛 `PermissionError`；而 `os.replace` 失败发生在
`st.save()` 里，异常沿调用栈抛到 `_worker`，`_worker` 的 `state.finish()` **又去写文件、
又失败**，被 `except Exception: pass` 吞掉 → 状态永远停在最后一次成功写入的快照上。

**修复**（三层）：
1. 临时文件名带 `pid + 线程 id`，互不覆盖；`os.replace` 失败退避重试 8 次
2. `RunState.save()` 改为**永不抛异常** —— 进度遥测失败不该搞死正在跑的任务
3. 状态文件在返回「已启动」**之前**同步落盘，避免前端先读到上一轮的完成态

**验证**：写了对抗性测试，一边以 **30ms 间隔**高频轮询状态、一边跑分析：

```
轮询次数: 59   读取失败: 0   耗时: 2.8s
最终状态 : done
save_error: None
六阶段: 全部 done
遗留 tmp : 无
```

### bug 2：`Active` / `Ended` 误判（上一轮修复，本轮复核）

时间窗口从 24h 换成 4h 时，275 条热点被误判为 `Ended`。根因是没限定比较范围。
已改为**同 geo + 同 window_hours 内**才比对，本轮多次运行复核通过。

---

## 6. 受限网络下的行为（已解除）

### 当时的现象

实测中环境的代理一度把 `trends.google.com` 拦成 `502 Bad Gateway`
（`example.com` 正常，说明是白名单拦截而非断网）。

这恰好暴露了架构上的必要能力：**采集失败必须降级，不能假装成功。**

### 解除后复测

用户放开网络后直连正常：`trends.google.com` 主页、RSS、`google.com` 三次探测全部 200
（约 1.1 秒），完整的六阶段运行 `source: live`。

### 降级路径刻意保留

它对这个产品是必要的 —— 目标机器随时可能遇到断网、代理、公司防火墙或 Google 侧限流。
现在的行为是：

- 线上抓取失败 → 自动回退到本地最近快照
- `status.json` 标注 `source: "cached"`
- 页面挂「缓存数据」黄色徽章，日志写明失败原因
- 其余五个阶段照常完成

---

## 7. 验证边界（没验证的部分）

要诚实说清哪些是「写完没实测」：

| 项 | 状态 |
|---|---|
| LLM 代码路径（请求 / 解析 / 降级 / 重试） | ✅ **已验证** —— mock 端点 36/36 |
| token 消耗量 | ✅ **已实测** —— 截获真实 prompt 量出，非估算 |
| 定时采集（开关 / 单次 / 停止 / 不污染分析状态） | ✅ **已验证** —— 接口与线程均实测 |
| 「更新数据」零 token 路径 | ✅ **已验证** —— 步骤 5-6 确实 skipped |
| LLM 真实模型的输出质量 | ✅ **已验证** —— 见第 9 节，已接真实密钥 |
| 跨天/跨小时的长期累积 | **未验证** —— 需要真的跑 3–7 天，没人能替时间加速 |
| 图表在超大数据量下的性能 | 未压测，目前 331 点无压力 |
| 移动端布局 | CSS 写了断点，未在真机验证 |

**关于快照期数的一个诚实提醒**：`persistence` 的单位是**采集批次**，不是小时。
只有固定等间隔采集（建议每小时 1 次）时，它才等价于「持续小时数」。
手动零散触发会让这个数偏大。看板页脚已写明这个口径，
`--reset-history` 可重建基线。

生命周期指标现在**技术上可用**，但要有业务意义必须等真实的小时间隔积累 ——
这不是代码问题，是物理时间问题，我不会用测试数据把它凑出来。

---

## 8. 下一步建议

**优先级 1：接上 LLM 密钥。** 这是唯一的「写完没验」项，也是让一句话需求真正好用的关键。

```bash
# 最省事的方式：复制 config/llm.env.example 成 config/llm.env，填两行
OPENAI_PROVIDER=deepseek
OPENAI_API_KEY=sk-你的密钥

python src/llm.py       # 自检：会真发一次调用，明确告诉你密钥能不能用
python server.py
```

> ⚠️ 上一轮我给的示例 `OPENAI_MODEL=deepseek-chat` **是错的**，这轮查证后已纠正。
> `deepseek-chat` / `deepseek-reasoner` 两个旧别名已于 **2026-07-24 下线**，
> 调用会直接失败。当前官方模型是 `deepseek-flash` 与 `deepseek-v4-pro`，
> 预设已填好，不用手写模型名。

**优先级 2：开定时采集。** 每小时 1 次，攒 3–7 天。主页一键开启即可。
记得先 `python src/scheduler.py --reset-history` 重建基线。
生命周期、每小时新增热点分布、类别趋势曲线这三个分析点才能立起来。

**优先级 3：未归类条目的实体识别（样本外 ≈23%，约 77 条）。** 若要继续压，正确做法不是堆词典，
而是加一次轻量 LLM 实体解析（人名 → 所属领域），每次约 40 条短文本，成本极低。
另外应给词典加**基于新数据的定期增量迭代**，否则新出现的专名会持续拉高未归类率。

**可选：** 时段对比（工作日 vs 周末）、品牌与外部销售数据打通、
把 `facts_related_queries` 从内嵌数组拆成独立表。

---

## 9. 接入真实密钥后的复验（2026-09-16 晚，M0 收尾）

用户按要求建好 `config/llm.env`（`OPENAI_PROVIDER=deepseek`）后，做了一轮真实复验。
**这一轮又挖出 3 个只有接真密钥才会暴露的问题** —— 都是 mock 测不出来的。

### 9.1 自检报「调用失败」，但错误详情是 `None`

`python src/llm.py` 打印：

```
调用次数 : 1（response_format 重试 0 次）
返回     : None
最后错误 : None          ← 最误导的一行
❌ 调用失败。请核对密钥、余额、模型名与网络。
```

`calls=1` 说明**请求其实成功了**（HTTP 200），但错误字段是空的，
用户按提示去查密钥/余额/网络，全查不出问题。

抓原始返回才看清真相：

```json
{"choices":[{"message":{
    "content":"",                                   // 正文空
    "reasoning_content":"我们需要回答用户。用户中文：…"},
  "finish_reason":"length"}],
 "usage":{"completion_tokens":20,"reasoning_tokens":20}}
```

**根因**：`deepseek-flash` 是推理型模型，思维链也计入 `max_tokens`；
自检里给的预算是 `max_tokens=20`，被思考全吃光，正文一个字都没生成。

**修了三处**：
1. `json_chat` 里把「HTTP 200 但正文为空」判为失败并**写明原因**（不再返回 None 了事）；
2. 检测到 `空正文 + finish_reason=length` 时**自动把预算翻倍重试一次**；
3. 自检预算 20 → 800，并打印 `finish_reason` 与重试原因。

新增 2 个回归场景（07 思维链吃光预算 / 08 持续空正文），断言数 28 → 36。

### 9.2 报告每次都要重试一次 —— 基准预算给少了

接入后跑真实分析，`status()` 显示 `retries=1`。加了 `last_retry_reason` 字段定位：

```
思维链吃光 max_tokens=2000（正文为空、finish_reason=length），已放大到 4000 重试
```

也就是说**每次生成报告都白白浪费一次调用**（第一次的推理 token 是真计费的），
而且慢一截。把报告叙述的基准预算从 2000 提到 4000（实测够用），
重试保留为兜底。修后：`retries=0`，端到端 **20.4 秒**（原 32.6 秒）。

> 教训：`max_tokens` 只是上限，**给宽不影响实际花费**（按实际生成量计费），
> 但对推理型模型给窄了就是真金白银的浪费 + 一倍延迟。

### 9.3 模型把「美国」当成筛选关键词

第一次真实分析，模型解析出的 plan 是：

```
"filters": {"categories": [], "keyword": "美国"}
→ SELECTED = 331 / 331      ← 全部命中，筛选形同虚设
```

数据源本身就是美国地区，`keyword="美国"` 会命中每一行。用户以为筛过了，其实没筛。

双保险修掉：提示词里明确「地区词不要填进 keyword」，同时在
`_llm_intent` 里加了一层兜底 `GEO_WORDS` 白名单剔除（不指望模型每次都听话）。
修后同一句话解析为 `categories=["Technology","Consumer"]`、`keyword=null`，
`SELECTED=9 / 331`。

### 9.4 顺带修掉的两个工程问题

**测试会覆盖真实数据。** `test_llm_path.py` 跑的是真实流水线，会写
`data/analysis.json`、`data/report.json`，还会往 `data/snapshots/` 里塞快照。
跑一次测试就把用户的分析结果换成 mock 内容。已加 `DATAPULSE_DATA_DIR`
环境变量让数据目录可覆盖，测试固定在 `tests/_tmpdata/`，
并加了一道**安全闸**：隔离没生效就中止，而不是继续跑。
已验证：跑完测试后真实数据文件指纹逐字节未变。

**Windows 上能同时起两个服务抢同一端口。** `HTTPServer` 默认
`allow_reuse_address=1`，在 Windows 上两个进程能绑同一个端口，第二个
「启动成功」但请求会随机落到先启动的旧进程 —— 我本人被这个坑了半小时
（改了代码、重启、复测，结论一直是旧的）。已改为 `allow_reuse_address=False`
并在启动前探测端口，被占用时明确提示「已经有一个在跑了」并退出（返回码 1）。

### 9.5 真实模型输出质量（终于能看了）

同一句需求（「最近美国有什么科技和消费领域的高增长热点？哪些品牌在爆发？」）
的真实产出：

- **需求解析**：正确识别出 `Technology` + `Consumer` 两个类别、`brand_only=true`，
  并生成了 3–5 个自己没有答案的追问（如「是新兴趋势、季节性波动还是短期事件驱动？」）
- **报告叙述**：每条都带具体数字且**与计算结果一致**，没有编造。
  例：「pokemon owala 与 discord outage 增长600%，peanuts collection 增长500%」——
  这些数字都能在 `dashboard.json` 里逐条对上。
- **建议**：可执行且带阈值，不是空话。
  例：「针对2条新兴信号（增长≥500%且搜索量≥5K）设置短周期复测清单」
- **边界意识**：主动写出「以上均为搜索关注度信号，不直接等同于销量或实际转化」——
  这条是 prompt 里的红线约束，模型确实遵守了。

结论：**LLM 作为「理解 + 解释」层是合格的**，数值层仍然全部由脚本产出，这条红线守住了。

### 9.6 数据状态说明

复验期间因测试与多次调试采集，攒了一批秒级连发的快照并污染了历史。
已用 `python src/scheduler.py --reset-history` 归档 21 期旧快照并重建基线
（归档不删除，在 `data/snapshots/_archive_<时间戳>/`）。

当前保留 3 期真实采集，`analysis.json` / `report.json` 为真实 LLM 产出
（`engine=llm`，筛选后 9 条）。

**正式开定时采集前，建议再 `--reset-history` 一次**，让基线干净。

---

## 10. 静态快照导出与在线发布（2026-09-16 晚）

### 10.1 需求

用户问：「我访问 `127.0.0.1:8848` 能用，但别人是不是打不开？我想放到个人主页让人也能看。」

这不是 bug —— `127.0.0.1` 是回环地址，全世界每台机器上的它都指向自己。要给别人看，
必须把数据搬到公网能到达的地方。三条路（内网穿透 / 云端发布 / 静态导出）里，
用户选了「只读浏览 + 要一个现成链接」，于是选 **静态导出 → 发布**：

- 只读 → 静态产物天然匹配，**没有密钥暴露风险**
- 现成链接 → 用内置发布能力一键上线，本机不用一直开机

### 10.2 核心问题：页面是用 `/api/*` 取数的

页面里写的是 `api('/api/meta')`、`href="/assets/theme.css"` 这类**根绝对路径**。
静态托管没有后端，直接导出会得到一堆死链 + 空白页。

做法是**只在 `api()` 函数内部做转换**，页面代码一行都不改：

```js
const STATIC_MODE = true;
async function api(path, opts) {
  const url = String(path).replace(/^\/api\/([A-Za-z0-9_]+).*$/, 'api/$1.json');
  const r = await fetch(url, ...);
```

于是 `/api/meta` 自动变成 `api/meta.json`。相应地：

- 资产引用 `/assets/x` → `assets/x`（相对路径，放域名根或子目录都不会断链）
- 导航 `/dashboard` → `dashboard.html`（静态服务器不认识无扩展名路由）
- `post()` 直接替换成「只读提示 + 抛错」，**产物里不存在任何写入口**
- 首页三个会写数据的按钮置灰、加 `title`，顶部插入「只读快照 · 数据截至 X」横幅

顺带把 echarts 从 CDN 下载到 `assets/echarts.min.js`：访客打开不再依赖外部 CDN，
更快也更稳（1 MB，可 `--no-echarts` 关掉）。

### 10.3 导出器自带三项体检

只写文件是不够的，必须能**自己发现问题**。`export_static.py` 每次导出都做：

| 体检 | 拦什么 |
|---|---|
| 安全体检 | 产物里出现 `.env` / `.py` / 原始数据文件 → 失败 |
| 取数体检 | `api/$1.json` 转换逻辑没装上 → 页面会空白 → 失败 |
| 路径体检 | 残留 `href="/` / `fetch('/api/` / 根路径跳转 → 换目录部署就断链 → 失败 |

任何一项不过就返回非 0 退出码，方便脚本化调用。

### 10.4 两个踩坑（值得记）

**① `--dump-dom` 会把 `<script>` 源码一起输出，导致断言假阳性。**
第一版验证脚本对整份 DOM 做关键字匹配，结果 index / dashboard / analysis 三个页面
全被报成「出现了『无法连接本地服务』」—— 而那句话本来就**写死在 JS 字符串里**
（错误分支的文案）。页面其实是好的，是检查方法错了。
修法：**先剥掉 `<script>` / `<style>`，只对渲染出来的文字做断言**。
这个坑对任何用 headless dump-dom 做验收的场景都成立。

**② 同一个页面，`--dump-dom` 超时，`--screenshot` 反而 9 秒就完事。**
看板页图表多、数据大，dump-dom 要等 DOM 完全空闲，180 秒都不够；截图模式不等这个。
验收"页面到底渲染出来没有"时，**截图比 dump-dom 更可靠**。

另外 `shutil.copy2` 不会创建目标目录 —— 往刚建好的 `dist/assets/` 复制文件直接
`FileNotFoundError`，得自己 `makedirs`。

### 10.5 验证结果

| 环节 | 结果 |
|---|---|
| 导出 | 9/9 接口落盘，1.62 MB，三项体检全过 |
| 纯静态环境（无任何后端） | 四页全部渲染正常（index 1524 / dashboard 20378 / report 4182 / analysis 2881 字符） |
| 线上实测 | 首页、看板、报告 HTTP 200；浏览器渲染零错误；echarts 本地加载 |
| 线上看板截图 | KPI 332 条 / 2.65M 搜索量 / 150% 增长，四象限、类别结构、品牌热度全部正常 |

**当前线上地址：https://us-trend-pulse.app.workbuddy.host/**

### 10.6 怎么更新线上数据

本机跑采集或分析 → `python export_static.py` → 重新发布 `dist/`。
访客看到的永远是"最后一次导出"的快照，这是只读方案的性质，不是缺陷。


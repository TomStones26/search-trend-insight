# ============================================================
#  🔐 密钥与本地配置 —— 最重要的一条，千万不要提交
# ============================================================
config/llm.env
config/*.env
!config/*.env.example
.env
.env.*
!*.env.example
*.key
*_key.txt

# ============================================================
#  📦 运行时数据 —— 由程序生成，不入库
#     仓库里不放 data/：它是采集/分析的产物，且会随运行不断变化。
#     首次运行会自动重建（点首页「更新数据」即可）。
# ============================================================
data/

# ============================================================
#  🐍 Python
# ============================================================
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/

# ============================================================
#  🧪 调试与临时产物（以 _ 开头的都是本次开发期的临时文件）
# ============================================================
_*
tests/_*
tests/_tmpdata/
_unused
*.log
*.tmp
*.bak
*.bak_*
*.orig
*.rej

# ============================================================
#  🖼️ 截图 —— 仓库只保留 README 用到的 4 张 + 1 张只读版首页
# ============================================================
demo/*.png
demo/dashboard-demo.html
demo/shots-static/*.png
!demo/shots-static/index.png

# ============================================================
#  🚀 说明：dist/ 是刻意保留的
#     它是 export_static.py 产出的「纯静态只读演示站」，
#     里面有页面 + 样式 + 数据快照，可直接用于 GitHub Pages 在线演示。
#     （每次重新导出会重建，属正常现象。）
# ============================================================

# ============================================================
#  🖥️ 编辑器 / 操作系统
# ============================================================
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
